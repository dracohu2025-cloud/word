{
  "name": "word-card",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "vercel:build": "vite build",
    "deploy:vercel": "bash ./scripts/deploy-vercel.sh",
    "deploy:vercel:prod": "bash ./scripts/deploy-vercel.sh --prod"
  },
  "devDependencies": {
    "vite": "^6.3.0",
    "vitest": "^3.2.4"
  },
  "dependencies": {
    "html2canvas": "^1.4.1"
  }
}
